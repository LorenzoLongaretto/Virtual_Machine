m1 equ "hdfgdf"
m2 equ "gfsd"
MOV DX, 0
MOV CX, 50
MOV AX, %000
SYS %3

mov ax,[0]
push ax
slen cx, [0]
sub cx,1
push [cx] ;la direccion del final
call palind
mov dx,m1 ;es pal
mov cx,1
mov [0], ax
mov ax,%1
sys 2
stop

palind: push bp
mov bp,sp
push ax
push cx
add ax,1
sub cx, 1
; cmp [bp+3],[bp+2] ; ax,bx
cmp cx, 0
jnz sigo
jmp fin

sigo: mov ax ,[bp+3]
mov cx ,[bp+2]
add [ax],1
sub cx,1
call palind
cmp ax,bx
jnz fin2

fin: pop bx
pop ax
mov sp,bp
pop bp
ret

fin2: mov dx,m1 ;no es
mov cx,1
mov [0], ax
mov ax,%1
sys 2
stop